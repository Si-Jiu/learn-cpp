{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "Build C++ File",
            "command": "clang++",
            "args": [
                "-g",
                "-Wall",
                "-std=c++20",
                "-o",
                "${workspaceFolder}/build/${fileBasenameNoExtension}.out",
                "${file}"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
